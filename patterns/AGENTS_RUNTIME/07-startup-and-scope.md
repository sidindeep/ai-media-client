## Startup And Scope Compatibility Index

This compatibility file contains no operational rules. Load only the matching
focused module:

- Startup checks, task goals, `gi start`, restore, sprint routing, and bootstrap:
  `patterns/AGENTS_RUNTIME/07-startup.md`
- Bug/PDF evidence, filesystem writes, repository cleanup, external paths, and
  scope boundaries: `patterns/AGENTS_RUNTIME/07-scope-and-evidence.md`

Update old routes to the focused file instead of loading this index plus both
modules.
